import React from "react";
export const metadata  = {
    title: 'Task | love-trick',
    description: 'love-trick',
}
export default function WhisperLayout({children}) {
    return <>{children}</>
}
